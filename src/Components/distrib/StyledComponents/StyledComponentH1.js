import React from "react";
import styled from "@emotion/styled";

const H1 = styled.div`
/* ${(props) => (props.primary ? `#4F8D85` : `#F8F3D5`)}; */
  color: #FFBC24;
  font-weight: 600;
  font-size: 48px;
  line-height: 57.6px;
  position: relative;
`;

export default H1;
